# 1С служебные
*.1CD
*.log
*.dt
*.pfl
DumpIB.zip

# Docker
docker/platform/*.deb
docker/platform/*.tar.gz
docker/platform/*.rpm
docker/platform/*.run
docker-compose.override.yml

# Отчёты
reports/*.xml
reports/*.html
reports/*.json

# Python
__pycache__/
*.pyc
.venv/

# IDE
.idea/
.vscode/settings.json

# OS
.DS_Store
Thumbs.db
docker/licenses/*.lic
# Артефакты сборки — пересоздаются пайплайном
build/
ib_test/
ib_mobile/
ib_managed/

# Готовые cf-файлы — результат compile, не хранятся в git
reports/*.cf

# Vanessa Automation (58 МБ, тянется через opm при необходимости)
tools/vanessa/vanessa-automation/

# Временные
*.tmp
*.bak

# Артефакты неправильных командных строк 1С (bash не распарсил ; в --ibconnection)
ib_managed;*
ib_*;Usr=*

# Лог deploy.py (генерируется при каждом запуске)
deploy.log

# Лог deploy.py (генерируется при каждом запуске)
deploy.log
